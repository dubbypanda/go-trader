# go-trader Project Context

## Environment
- Go 1.26.2; full binary path if not on PATH (`/opt/homebrew/bin/go`).
- Python via `uv run --no-sync python` for dev/backtest/manual CLI; Go subprocess (scheduler) calls `.venv/bin/python3` directly — deterministic after `uv sync`, no systemd PATH config. Worktrees: `uv sync` once per checkout.
- **systemd:** `go-trader.service`, `systemd/go-trader@.service` use `ProtectSystem=strict`; no `PATH`/`UV_CACHE_DIR` injection. `/opt/go-trader/.env` (or per-instance) sets `DISCORD_BOT_TOKEN` + secrets. **#1056** runtime config lives OUT of the deploy tree at `/var/lib/go-trader[/<instance>]/config.json` (writable via `StateDirectory=go-trader[/%i]` — the daemon rewrites config in place, so the directory must stay writable under `ProtectSystem=strict`); `ExecStart … --config <that path>`. `WorkingDirectory` stays the code checkout. `scheduler/config.json` is a transition **symlink** → that path; migrate with `scripts/migrate-config-out-of-tree.sh`.

## Setup
- `uv sync`; copy `scheduler/config.example.json` → `scheduler/config.json`, fill API keys. **#1056 (deployments):** see Environment above for the out-of-tree layout — stop the service, run `scripts/migrate-config-out-of-tree.sh` once (refuses while the daemon is live, else a config write in the migrate→restart window clobbers the symlink into an in-tree file), then point `ExecStart --config` + `StateDirectory` at it.

## Decision Priorities
- **Always pursue the absolute best technical solution — full stop.** Cost, compute, resources, time-to-implement, effort, manpower, tests, and code volume are NOT design constraints and must never narrow the option space: choose the best solution as if these were unlimited, then update/rewrite tests after the feature is done. These govern *quality*, not *scope*; don't override the branch+PR workflow, "verify issue claims against code", or destructive-action safety rules.
- **Never give time, duration, or effort estimates** ("2–4 days", "low effort", headcount/manpower) in responses, PRs, issues, or commits. Describe complexity only in terms of scope and risk.

## Repo Structure
`scheduler/` is one Go `package main`. **Guardrails only below — full file-ownership map, symbol/config inventories, and per-feature mechanics live in `docs/ARCHITECTURE.md` (not auto-loaded; read the relevant section there before editing a subsystem).**
- `executor.go`/`shutdown.go` — **New side-effecting wrapper → `runPythonSideEffect`, NEVER `runPython`** (`pythonSemaphore=4`, `scriptTimeout=30s`, drain `shutdownDrainCap=15s`→SIGKILL).
- `server.go`/`ui_*.go` (`ui_server`,`ui_tuner`,`ui_candles`,`ui_reports`,`static/ui/*`) — **Lock order `mu → strategiesMu`** (reusing `mu` deadlocks SIGHUP). **Loopback only**; `/health` 503 while draining. POST `/config`: `requireMutatingAPIAuth`(`status_token`)+`requireSameOrigin`; `configWriteMu` serializes writes; `applyStrategyConfigPatch` needs `config_version>=13`. `DefaultStatusPort=8099` (+5 fallback).
- `config.go`/`config_migration.go` — `CurrentConfigVersion=16`. **Seven mutually-exclusive HL stop fields** (all-omitted→`DefaultStopLossATRMult=1.0`). **New close evaluator → `closeStrategyOwnedKeys`.** **Single** `*StrategyRef` (rejects `close_strategies` len>1; `close_strategy` canonical). Unknown-key guard. `strategyUsesTieredTPATRClose(sc)` gates on-chain TPs — NOT `len(tiers)>0`. **#1048** `CircuitBreaker *bool` read via `CircuitBreakerEnabled()`, never directly — hot-reloadable while open. **#1273** CB timing/threshold overrides (`cb_drawdown_cooldown_minutes`/`cb_loss_streak_threshold`/`cb_loss_streak_cooldown_minutes`) read ONLY via the `CircuitBreaker*` accessors; `RiskReasonConsecutiveLosses` is a threshold-free PREFIX — classifiers `strings.HasPrefix`, never equality. **#1135** canonical operator defaults live under `user_defaults.{close,regime_atr,manual}`; legacy top-level aliases migrate on load.
- `close_defaults.go` — **#866/#1135** three-layer resolution (system→user→strategy); explicit `tp_tiers` wins; reserved `regime_atr` section for standalone `*_atr_regime` `use_defaults` owners (#1134). **#1133** `user_defaults.close["trailing_tp_ratchet_regime"]` may carry the coupled `trailing_stop_atr_regime` SL owner — `applyUserCloseDefaultRatchetRegimeTrails` runs in `loadConfig` **before** the scalar ATR-stop default (else the perps acquires `stop_loss_atr_mult` first, then fails single-owner validation). **Mechanics → ARCHITECTURE.md.**
- `state.go`/`db.go` — SQLite-only (`modernc.org/sqlite`); idempotent migrations. `ValidatePerpsDirectionConfig` startup check; `CheckStatePresence` (`GO_TRADER_ALLOW_MISSING_STATE=1`).
- `risk.go`/`strategy_interval.go` — `CheckRisk` skips `manual`. **#1009** corrupt position (qty≤0 OR avgCost≤0) → **zero-PnL** `*_corrupt` close leg, cash untouched (`closePositionIsCorrupt`/`bookPerpsCloseWithFillFee`). **#1008** `classifyPositionTradeType` labels force-close legs (operator-display only).
- `daily_loss.go` (#1269) — portfolio-wide daily loss limit (`portfolio_risk.daily_max_loss_usd`/`daily_max_loss_pct`, 0=off; both set → lower resolved USD wins; pct basis = Σ strategy `initial_capital`, 0-basis loudly inert). **Hold-only, UNLATCHED pure read** — `evaluateDailyLossLimit` per cycle under the kill-switch RLock; self-clears at UTC rollover, survives restarts via persisted `DailyPnL`. Measures PRE-FEE realized PnL; never force-closes. **New `portfolio_risk` gate → copy this shape** (#1270 did): pure-read eval under the kill-switch RLock, #1150 `pausedBlocksSignal`-derived holds at the 6 dispatch sites + options filter, manual refusals via `manualStateView` (manual_core.go — dispatch sites never see CLI/dashboard-driven manual entries), hot-reload via `clonePortfolioRiskConfig`. **Mechanics → ARCHITECTURE.md.**
- `exposure_cap.go` (#1270) — same-direction exposure cap (`portfolio_risk.max_same_direction_notional_usd`/`max_asset_concentration_pct`, 0=off). **Blocking-only + direction-aware**: `exposureCapBlocksSignal` (= `pausedBlocksSignal` AND signal-sign matches blocked direction) at the 5 crypto dispatch sites + options coarse-delta filter + manual open/add/limit-open refusals (BOTH arms — concentration basis via `manualExposureCapStatus`, PVBasisMiss warning surfaced); TS futures site deliberately ungated (not in the crypto bucket). **Single exposure model** — `computeAssetDeltas` (correlation.go) shared with `ComputeCorrelation`: includes `manual`, AvgCost fallback on missing price, unpriceable/corrupt legs fail-safe EXCLUDED (never block-everything/nothing). Hot-reloadable via SIGHUP (deliberately unlike `max_notional_usd`). **Mechanics → ARCHITECTURE.md.**
- `hyperliquid_fills.go` — fill resolver built **outside `mu.Lock`** (failure→modeled fee); `HLFillLookup.Px` is VWAP.
- `pause.go` (#1150) — `StrategyConfig.Paused` (`"paused"`); NOT a `dueStrategies` skip, full manage-only cycle. `pausedBlocksSignal(...)` holds position-increasing signals (fresh open/add/flip) at all 6 regime-gated dispatch sites; close-registry/pure-close exits + trailing SL/ratchet/protection sync pass through. Hot-reloadable always incl. while open. Surfaces `[config]`/`inspect`/`/status paused`/Discord `⏸️`. **Mechanics → ARCHITECTURE.md.**
- Regime cluster (`regime*.go`, `post_tp_sl.go`):
  - `stampPositionRegimeIfOpened` runs on **all 5 execute dispatches**; **regime-store failure → fail-open `regime=-`, NO inline fallback** (#879) — display/management only; the ENTRY gate's failure policy is configurable (#1278, next bullet).
  - **#1278** `regime_gate_on_failure` (`"open"` default | `"closed"`; per-strategy over global `regime.gate_on_failure`) — `allowed_regimes` gate policy for an EMPTY gate label (store failure/sealed budget/missing window). Fail-closed holds FRESH opens only: `posQty>0` management, closes, and ungated strategies untouched by construction. **Read via `resolveRegimeGateOnFailure(sc, rc)`, never directly.** Hot-reloadable always incl. while open; `closed`+gate+`regime.enabled=false` rejected at load (permanent block). Backtest parity `Backtester(regime_gate_on_failure=…)`/`--config`; `init` emits `closed` on wizard-gated strategies. **Mechanics → ARCHITECTURE.md.**
  - `regime_atr.go`: **Critical:** `parseRegimeTPTiers` must strip `sl_after` before ATR-block parse — else load failure AND silent never-arm. **v15** `atr_multiple` canonical (both-set rejected); new `sl_after` surface → `closeStrategyOwnedKeys`.
  - `regime_unified.go` (#841): block **owns the SL** (`validateUnifiedCloseSoleOwner` rejects strategy-level stop fields).
  - `regime_directional_policy.go`/`regime_directional_certification.go`: HL perps, `regime.enabled`; mutates **local** `sc`; #1025 backtest parity; #822 orphan auto-close (outside `mu`, 90s). **#1085 DEFAULT-OFF:** directional→BASE unless `(asset,timeframe,classifier)` certified per-state; contradicting/uncertified→BASE. **#1124 cert lookup is exact-match, NO family fallback** (a bare `ranging_directional` cert does NOT certify `_up`/`_down`; fail-closed) — unlike the bare-covers-subs gating rule elsewhere. FLAT=live verdict; OPEN=`DirectionCertifiedStatesAtOpen` frozen at entry. Artifact `regime_directional_certifications.json` (empty per #1076); fail-closed load. **#1157** owner DM'd on DEFAULT-OFF/EXPIRED cert lines; `/status` `EffectiveDirection` gated through the cert check. **Mechanics → ARCHITECTURE.md.**
  - `regime_profile_allocation.go` (#998): **flat-only** profile freeze (`Position.OpenProfile`); `applyRegimeProfileParams` **never mutates cfg**.
  - **#1189** `strategyDisplayRegimeLabel` is a **display-only** regime resolver for status/dashboard (does NOT repoint `stratState.Regime`, the live trading fallback). **Detail → ARCHITECTURE.md.**
  - `regime_dynamic_tp_sl.go` (#843) / `regime_divergence.go` (#907): HL perps live only, backtester rejects; SIGHUP-blocked while open.
  - `regime_transitions.go` (#1224): **alerting-only** — never gates entries, mutates config, or touches positions; runs post-`regimeStoreReady()` outside `mu`, fail-open on store/DB failure (#879 convention). Per-window transition history + bar-accurate debounce (`processRegimeTransitionAlerts`/`netRegimeTransition`) + cross-window reversal-pattern alerts (`processRegimeReversal`); three SQLite tables (`regime_window_history`/`regime_window_transitions`/`regime_reversal_alerts`). New `regime.transitions` config block (`enabled`/`debounce_cycles`/`retention_days`/`reversal_min_opposing`). `/status` note + `GET /api/regime/transitions`. **Mechanics → ARCHITECTURE.md.**
- `trailing_tp_ratchet.go` — **NO on-chain TP** (EXCLUDED from `isTieredTPATRCloseName`/`strategyUsesTieredTPATRClose`/suppression). SL owner `trailing_stop_atr_mult` (scalar) / `trailing_stop_atr_regime`. **Scope: HL perps + `manual`**; hot-reload blocked while open. Tier-clear-and-tighten owner-DM (idempotent on `SLAdjustedTiersProcessed`), gated by two-layer `notify_ratchet_triggers` (#1118, nil→enabled, per-strategy overrides global); notification-only so hot-reload works even while open. **Mechanics → ARCHITECTURE.md.**
- `llm_entry_analysis.go`/`shared_scripts/llm_review.py` (#1137) — **advisory-only** post-open LLM entry commentary; per-strategy `llm_entry_analysis` (default off, hot-reloadable always). Dispatch `queueLLMEntryAnalysisIfOpened` at the 5 execute-apply sites — fresh opens only, idempotent via `Position.LLMAnalysisRequested`. **Dedicated lane:** worker spawns via `spawnPythonProcess` (semaphore-free) — NEVER `runPython*`. Verdict → `Position.LLMVerdict` → `trade_diagnostics.llm_verdict` at close (see #1147 below — sole writer). Go re-enforces verdict vocab + 55-word cap. `ANTHROPIC_API_KEY`; probe gated on any strategy opted in. **Mechanics → ARCHITECTURE.md.**
- `scale_in.go` (#873) — **Frozen SL/TP geometry** via `Position.RiskAnchorPrice` — all ATR-geometry/trigger sites read anchor, NOT blended AvgCost. Add leg excluded from `#T`. HL perps+manual; **NOT backtested**.
- `manual.go`/`manual_limit.go`/`manual_sl.go` (#873/#883/#1050/#1115/#1121/#1131/#1140) — manual CLI (kill-switch+CB); HL perps/`manual`. SL edits cancel-then-queue `PendingManualAction` (NEVER direct UPDATE); fail-closed via `manualSLAutoManaged`/`pendingSLActionExists`. Limit partials fail closed on foreign position. `force-close <id>` — **live HL `type=perps` only** — reduce-only close, refused while an SL edit is queued. **Full mechanics → ARCHITECTURE.md.**
- `hyperliquid_open_trailing.go` (#885) — `armTrailingStopAtOpenNow` arms initial SL inline at open (no naked gap); perps live+paper.
- `hyperliquid_protection.go` — reduce-only TP/SL for `perps`+`manual`; requires `pos.EntryATR>0`. **On-chain TP gate = `strategyUsesTieredTPATRClose(sc)` AND `hyperliquidIsLive(sc.Args)` — paper always false.** `defaultHLProtectionTiers()` single source (mirrored in `.py`).
- `version_probe.go`/`probe_cmd.go`/`exit_codes.go` — **New runtime-required CLI flag → append to both probe argvs** (`probeArgv`+`executeProbeArgv`). `runProbe` exits `ExitProbeFailure=78`; `RestartPreventExitStatus=78` in both service files.
- `trade_diagnostics*.go`/`diagnostics_cmd.go` (#1147) — **diagnostics-only** per-close trade-quality rows (`trade_diagnostics` table): EAGER identity insert inside `recordClosedPosition` (hook mirrors `tradeRecorder`, NEVER the SaveState buffer); MFE/MAE/capture computed async OUTSIDE `mu` (`fetch_candles.py`, failure→NULL metrics + status, never blocks a close). `llm_verdict` reserved for #1137 — never written here. Report `go-trader diagnostics` — see SKILL.md. **Mechanics → ARCHITECTURE.md.**
- `agent_info.go` (#1051) — `agent-info` **read-only** JSON dump; `--bootstrap-md`→`AGENTS.generated.md` (NEVER `AGENTS.md`); temp-copy config + state DB `mode=ro`. New subcommand→`knownSubcommands`+capability registry; new `os.Getenv`→env-var registry.
- `failure_alerts.go`/`script_failure_alerts.go` — **New `run*Check` → wire both helpers (`notifyLiveExecFailure`/`notifyScriptFailure`) + thread `notifier`.** Primary alert at `scriptFailureAlertThreshold=3`. **#1128:** `scriptFailureErrorIsTransient` (tight 429/rate-limit regex — no bare `429`) → WARN + separate `scriptFailureTransientTracker`; operator DM at 15 consecutive OR `scriptFailureTransientAlertMaxDuration=75m` wall-clock; `clearScriptFailure` clears both.
- `discord_commands.go`/`discord_mutating_commands.go`/`discord_regime_gate_command.go`/`closing_strategies.go` — mutating cmds owner-DM-only; **New mutating command → `opsCommandNames`+`slashCommands()`+dispatch (bare ID).** `/remove-strategy`+`/paper-to-live`+`/apply-regime-gate` (#1205) need `AskDM` confirm. Config writes go through `writeValidatedConfigRoot`/`applyStrategyConfigPatch` on `configWriteMu`. `/apply-regime-gate`'s `regime.enabled` flip can reactivate another strategy's dormant `allowed_regimes` gate — re-check the blast radius just before the write, not just at confirm-time. `/closing-strategies` (#1203) is **read-only**. **Mechanics → ARCHITECTURE.md.**
- `shared_wallet_reconcile.go`/`shared_wallet_drift_alerts.go`/`trade_pnl.go`/`backfill_trade_ledger.go` (#918/#954/#921) — trades store **PRE-FEE** `realized_pnl` + always-stamped `exchange_fee`; consumers read **net** via `tradeNetPnL`/`tradeNetPnLSQL`/`tradeLedgerDeltaSQL`. `SharedWalletDriftTracker`: $0.01 tol, 2-cycle confirm. **Mechanics → ARCHITECTURE.md.**
- `cashflow_journal.go`/`okx_cashflow_journal.go`/`topstep_cashflow_journal.go` (#1100-#1106) — exchange settled-cash journal; **HL total-drift LIVE** (`driftBasisJournal`); fail-closed to trade-ledger when unusable/`GO_TRADER_CASHFLOW_JOURNAL_ALARM=0`; OKX/TopStep shadow-only; OUTSIDE `mu`. Journal=TOTAL; trade-ledger=per-strategy attribution.
- `hl_reconcile_gap_alerts.go` (#971/#974) — fail-closed shared-coin SL-gap operator alerts. **Alerting only — never books/guesses/mutates.**
- Also (detail in ARCHITECTURE.md): `portfolio_warning.go`(#904)/`circuit_breaker_alert.go`(#905) enriched DMs; `backfill_hl_fees.go`; `hyperliquid_trailing_stop.go`; `portfolio.go`; `kill_switch_close.go`+`*_close.go`; `*_marks.go`/`deribit.go`; `init.go`,`sharpe.go`,`correlation.go`,`leaderboard.go`,`notifier.go`/`telegram.go`,`updater.go`,`pricer.go`,`tradingview_export.go`,`config_reload.go`.

Other dirs (guardrails; inventories in ARCHITECTURE.md):
- `shared_scripts/` — per-platform `check_*.py` + `check_{strategy,options,price,regime}.py`, `strategy_tuner_schema.py`, `simulate_strategy.py`. All accept `--regime-payload-json` (#879). All probed at startup when any strategy configured.
- `platforms/<name>/adapter.py` — one `*ExchangeAdapter`/file, class `endswith("ExchangeAdapter")`. HL meta/OHLCV caches in `/tmp` (per-instance, `PrivateTmp=true`); gap-margin + extend-until-limit fetch (#937/#947). **#1128:** SDK `Exchange` lazy via `_ensure_exchange`/`_require_exchange` (regime/OHLCV-only subprocesses skip duplicate spotMeta/meta); cached meta passed into init; 30s backoff on init failure. HL indices sparse — `_normalize_spot_meta` rebuilds dense.
- `shared_tools/` — `regime.py`,`funding_fetcher.py`(#960, `merge_asof` **backward** only; **#1176 `funding_coverage` is a set of DISJOINT intervals per (exchange,coin)** — `store_funding_coverage` merges only overlapping/touching ranges, NEVER min/max union; `init_db` migration drops pre-interval rows, refetch is the only recovery),`hl_user_fills.py`,`storage.py` (lazy-init, import side-effect free for probe). **ATR has 5 sites** (`atr.py:standard_atr` + 4 inline copies incl. `open/analog_retrieval.py` #1138 — keep in sync); `standard_atr` rounds to int only when `atr>=100`. **Detail → ARCHITECTURE.md.**
- `shared_strategies/` — open source of truth `open/registry.py`; **`open/{spot,futures}/strategies.py` are shims — do not edit.** Close `close/registry.py`; options `options/strategies.py`.
- `backtest/` — `backtester.py`,`optimizer.py`,`run_backtest.py`,`backtest_{options,theta,pairs}.py`,`parity_diff.py` (#906/#950). See § Backtest.

## Key Patterns
- Run git from repo root. Prefer `go -C scheduler build .` over `cd scheduler &&`.
- **New platform (8):** (1) `adapter.py`+`__init__.py`, (2) `check_<name>.py`, (3) `executor.go`, (4) `config.go` (prefix+validation), (5) `fees.go`, (6) `main.go` dispatch, (7) `init.go`+`generateConfig`, (8) `pyproject.toml`. New options: adapter `get_{vol_metrics,real_expiry,real_strike,premium_and_greeks}` + `check_options.py` + `CalculateOptionFee` + `OptionPlatforms`.
- Adapters via `importlib`, class `endswith("ExchangeAdapter")` (one/file); check scripts use public methods only.
- **Close registry import:** never `import registry` directly (collides w/ open) — use `from close_registry_loader import evaluate, list_strategies, build_close_registry` (in `shared_tools/`).
- Subprocess contract: scripts emit JSON to stdout even on error; exit 1 on error; Go parses regardless of code.
- **State locking:** `mu sync.RWMutex`. 6-phase loop: RLock → Lock(CheckRisk) → no-lock(subprocess) → Lock(execute) → marks → RLock(status). Audit: `grep -n "mu\.\(R\)\?Lock\(\)" scheduler/main.go`.
- Platform dispatch: use `s.Platform == "ibkr"`, never ID prefix. ID prefix map `hl-`/`ibkr-`/`deribit-`/`ts-`/`rh-`/`okx-`/`luno-`, else BinanceUS.
- Strategy types `spot`,`options`,`perps`,`futures`,`manual`. Perps paper reuses `ExecuteSpotSignalWithFillFee`, live calls `RunHyperliquidExecute` first; futures `ExecuteFuturesSignalWithFillFee`. **Manual** auto-fills `script`/`args[0]="hold"`/interval; **#1115** close default is conditional: regime-enabled + a default per-regime trail block resolves for the active classifier → `trailing_tp_ratchet_regime`, else `tiered_tp_atr_live` + SL@2.0×ATR; explicit `close_strategy`/stop-field overrides keep the scalar path (`resolveManualRatchetRegimeTrailBlock`, tunable via `user_defaults.manual.trailing_stop_atr_regime`). `CheckRisk` skipped; shared-coin peers guarded by owner checks.
- **Open/close split:** `OpenStrategy` overrides entry; **single** `CloseStrategy` owns exit (nil → open-as-close; wire `"closes"` still length-≤1); close before open; options not split. **Partial-close** decrements `pos.Quantity`, preserves `InitialQuantity`, suppresses SL cancel/replace. Position-aware via `--position-*`/`PositionCtx`; `def *_strategy` must name framework kwargs explicitly.
- **Live exec guard:** every main.go dispatch uses `liveExecFailed`; `runXxxExecuteOrder` returning `ok2=false` → skip state update.
- **Skip-reason guards:** `run{Hyperliquid,OKX,Robinhood,TopStep}ExecuteOrder` must repeat `ExecuteXxxSignal`'s conditions **before spawning** (else on-chain fill, no Trade record). Use `{Perps,Spot,Futures}OrderSkipReason`; capture `posSide` with `posQty` in Phase 1 RLock (`Position.Quantity` always positive).
- **Bidirectional perps:** short-entry strategies must be in `bidirectionalPerpsStrategies` (init.go); flip sizing `perpsLiveOrderSize`, catastrophic flip → close-only. **Roster + #1009 flip-safety mechanics → ARCHITECTURE.md.**
- `dueStrategies` value-copied from `cfg.Strategies` — mutations don't persist; update `cfg.Strategies` first.
- Notifications: add methods to `MultiNotifier` (not `backends`); guard new backends behind nil-checks. Channels keyed `spot`/`options`/`<platform>`/`<platform>-paper` (+`*.TradeAlertChannels` overrides); `stratType` reroutes across platforms; SIGHUP-reloadable.
- HL: add `platforms/hyperliquid/` to sys.path before `from adapter import HyperliquidExchangeAdapter` (SDK clash); funding rates `info.meta_and_asset_ctxs()` NOT `info.meta()`. Fees `CalculatePlatform{Spot,Futures}Fee`.
- **Position ownership:** `Position.OwnerStrategyID`; `syncHyperliquidAccountPositions` reconciles owner only; shared-coin reconcile non-destructive for ambiguous gaps. **SL attribution** `hlReconcileSLFillConfirmed` (exact OID+qty) else `hl_sync_external`.
- **State SQLite-only** (`scheduler/state.db`, `cfg.DBFile`). `RecordTrade`→`StateDB.InsertTrade`; `ClosedPositions` flushed by `SaveState`; leaderboard `BuildLeaderboardMessages`.
- **Trades schema:** `is_close`/`realized_pnl` flag close legs; `DeferredOpen` runs protection-sync before `RecordTrade`. `LifetimeTradeStatsAll` `#T` counts opens, W/L grouped by `(strategy_id, position_id)`. HL real fee `lookup_fill_fee_by_oid` (failure → modeled). **`HLFillLookup.ClosedPnLGross` is gross — never wire into `Trade.RealizedPnL`.**
- **HL kill-switch fill accounting:** shared-coin fills split by virtual qty; `hyperliquidKillSwitchFillShare` **fails closed** when not a peer. **Mechanics → ARCHITECTURE.md.** **Close-side:** `closeTradeSide` short→`buy` else `sell`; `optionCloseTradeSide` sell→`buy` else `sell`.
- **Map iteration:** ALWAYS `sort.Strings(keys)` before formatting operator-facing or test output — Go map iteration is randomized.
- **Regime detection** (`shared_tools/regime.py`): `adx` default (3 labels) / `composite` opt-in (9 labels). **#1124** composite `ranging_directional` splits by `return_eff` sign into `ranging_directional_up`/`_down`; **one-directional family rule** — a bare `ranging_directional` configured anywhere covers its `_up`/`_down` subs for entry gating, exhaustiveness validation, and runtime resolution, never the reverse (exact match wins, bare fallback only on miss). Ratchet/ATR-trailing defaults route `_up`/`_down` to the `ranging_directional` group (else nil tiers → silent never-arm). **Window/composite/threshold mechanics → ARCHITECTURE.md.**
- **Strategy registries:** new open strategy → (1) `open/registry.py`+`PLATFORM_ORDER`, (2) `knownShortNames`+defaults in `init.go`, (3) `DEFAULT_PARAM_RANGES` in `backtest/optimizer.py`. Use `variants={"futures":{...}}`. **Deprecated:** `DISCOVERY_HIDDEN_STRATEGIES` — hidden from discovery, stay registered for explicit configs/backtests. **#1275:** the M5 fee-audit `deprecate` roster (`M5_DEPRECATED_EDGE_STRATEGIES`, 26 names, `edge_status="deprecated_m5"`) is unioned in; the operator warning is **Go-only** — `scheduler/edge_status.go` drives the startup `edge=deprecated_m5` tag + one-time owner DM (`allow_deprecated: true` silences the DM, tag stays `(ack)`; hot-reloadable always — masked in `strategyRestartShape` + applied in `applyHotReloadConfig`, and `reloadConfig` re-fires the warning via `newlyDeprecatedEdgeWarnings` when a reload moves an open leg onto an M5 name or drops the ack); NO per-cycle Python warning (check scripts run every cycle and can't see the ack). Keep both rosters identical (parity test). No config-generation surface (starter default, wizard pre-select, discovery-failure fallback lists in init.go) may offer a discovery-hidden name (guard test). **#1138 `backtest_only=True`** marks a strategy hard-live-ineligible (distinct from merely-hidden): `reject_backtest_only_strategies` fail-closes it out of all 5 live check scripts + the open-as-close fallback — e.g. `analog_retrieval`. **Before refactoring registry: snapshot `--list-json` and diff after — Go `discoverStrategies` needs byte-identical output.**
- Strategy DSL: `apply_strategy` merges config params UNDER runtime (runtime wins); `HTFFilter` not on options/`delta_neutral_funding` (perps-only); perps & futures `Position` have `Multiplier>0`, only perps `Leverage>0`.
- **Per-strategy CB pending close:** `RiskState.PendingCircuitCloses`; HL CB drain clears pending without on-chain close when `hlLiveStrategiesForCoin(sym)`>1. **Operator-required** (`OKXSpot`,`RobinhoodOptions`): CRITICAL log + notifier per cycle until reset.
- **CB enable/disable (#1048):** `CircuitBreakerEnabled()` false suppresses BOTH firing arms (drawdown AND loss-streak) for live+paper; an already-latched CB/pending close still drains. `cb=off` in startup summary + inspect. **#1273** cooldowns/threshold per-strategy-tunable (defaults 24h/5/1h); the loss-streak accessor drives firing arm AND suppression warning. **Mechanics → ARCHITECTURE.md.**
- **Latched CB manage-only (#1046):** a latched (not first-fire) CB on an HL perps strategy with an open position falls through with `result.Signal=0` instead of `continue` — keeps trailing-SL/TP ratchet + protection-sync running. HL perps only; first-fire cycle excluded. **Mechanics → ARCHITECTURE.md.**
- **Kill switch:** `planKillSwitchClose(KillSwitchCloseInputs)`→`KillSwitchClosePlan{OnChainConfirmedFlat}`. New platform = add fields + close/fetcher pair. **#1190** reset DM now carries drawdown reason, instance/wallet identity, and a protection-gap warning when not confirmed flat. **Mechanics → ARCHITECTURE.md.**
- `initial_capital`: only writer `StateDB.SetInitialCapital`; `SaveState` refuses stale overwrites.
- **HL perps stop-loss:** `EffectiveStopLossPct(sc)` — seven mutually-exclusive owners (config.go); scalar↔regime AND nil↔positive blocked on hot-reload while open. HL caps 1000 triggers/acct. **Priority order → ARCHITECTURE.md.**
- **Risk-per-trade sizing (#1268, `risk_sizing.go`):** opt-in `risk_per_trade_pct` (HL perps only) — `qty=(cash×pct/100)/stop_distance`, capped `cash×exchange_leverage`; sizing travels as `PerpsSizing` (`PerpsSizingFor`) through `perpsLiveOrderSize`/`executePerpsSignalWithLeverage`. Mutually exclusive with `sizing_leverage`/`margin_per_trade_usd`/`allow_scale_in`; regime-resolved stop owners rejected at load. **Fail-closed:** unresolvable stop distance refuses the open (never notional fallback); flip degrades close-only; risk↔notional switch blocked while open. Backtester parity via `Backtester(risk_per_trade_pct=…)` (fraction capped 1×, pct owners rejected under `--config`). **Mechanics → ARCHITECTURE.md.**
- **HL trailing stop-loss:** cancel+replace when trigger moves > `TrailingStopMinMovePct` (0.5%), ATR-mult frozen at open; **SL size cap `hlSLEffectiveQty=min(virtualQty,onChainQty)`**; `armTrailingStopAtOpenNow` no naked gap. The lock-free protection walker's `hyperliquidProtectionPositionSnapshot(pos)` MUST carry the full protection surface (regime label/windows + frozen #873 anchor) or paper regime trailing SLs go unarmed (#1015). **Routing detail → ARCHITECTURE.md.**
- **HL margin:** peers on same coin need shared `margin_mode`+`leverage` (`hyperliquidPeerStrategyErrors`); `update_leverage` from flat only; shared-coin reconcile books only exact OID-confirmed SL fills. **Detail → ARCHITECTURE.md.**
- **HL on-chain protection suppression:** `strategyConfigWithOnChainProtectionFilter(sc)` nils the single `CloseStrategy` for live tiered-TP (prevents limit-fill/close-evaluator race). **Paper never suppressed.** New ATR-tiered evaluator → add to `closeStrategiesSuppressedByOnChainProtection`.
- **SIGHUP hot reload** (`applyHotReloadConfig`): `validateHotReloadCompatible` blocks add/remove, script/args/type/platform/HTFFilter, kill-switch identity, DB path; `validateHotReloadStateCompatible` blocks state-shifting fields **while open**. `invert_signal` (HL perps/manual) flips BUY↔SELL before `Direction`. **Blocked-field list → ARCHITECTURE.md.**
- **Adding a per-strategy config flag:** (1) field on `StrategyConfig`, (2) main.go `run*Check` appends CLI flag, (3) parse in each Python check script, (4) `InitOptions`+wizard+`generateConfig`. **If *required* by runtime CLI, also append to `probeArgv` in `version_probe.go`** (asymmetric deploy fails at startup).

## Pull Requests
- Reference issue with `Closes #<N>` in body. In GitHub comments avoid `#N` for list items (auto-links); use `1.`.
- Fetch latest bot review: `gh api repos/richkuo/go-trader/issues/<N>/comments --jq '[.[] | select(.user.login=="claude[bot]" or .user.login=="github-actions[bot]")] | last | .body'` (top-level summary on issues endpoint, not pulls; #1178 review runs post as `github-actions[bot]`, older reviews as `claude[bot]`).
- Before merging long-running PR: `git fetch origin main && git diff origin/main..HEAD -- <paths>` catches silent reverts.
- **Commits and PR bodies:** end with `LLM: <model> | <effort> | Harness: <action>` (default Claude Code footer slot). Do **not** append any `Co-authored-by:` / `Co-Authored-By` trailer.

### PR review format (`@claude review`)
**SSoT: `.github/prompts/pr-review-format.md`** (edit prompt file, not workflow string). First line exactly `LGTM` or `Needs Updates` (blocking = `### Needs Fixing` / `### Requires Human Review` only). Safety findings never dropped. Needs Fixing/Optional require **Invariant:** + **Must survive:** (1–3 cases). Prefer in-PR fixes; follow-up issue only when genuinely out-of-scope. **Reviews never gate on or wait for CI** — CI intentionally outlasts the review; branch protection enforces it separately (no `gh pr checks`/`gh run *` reader, no `actions: read`).

### The Claude Code workflow itself (`.github/workflows/claude.yml` + `claude-run.yml`)
- **#1178 least-privilege split:** `claude.yml` = `classify` job + two caller jobs `uses:`-ing the reusable `claude-run.yml` body; **only the call-site `permissions:` differ.** Review job: `contents: read`+`pull-requests: write`+`issues: write`, **no `id-token: write`** (else injected code could mint the Claude App token via OIDC). Implement job keeps `contents: write`+`id-token: write`.
- **Token model:** git/gh ops run on the **Claude GitHub App installation token** — job `permissions:` do NOT cap it. Review job passes `github_token: ${{ github.token }}`; comments post as **`github-actions[bot]`** (`BOT_LOGIN`), implement stays `claude[bot]`. Patch steps pass `RUN_ID` — only comments embedding this run's link qualify.
- **Mode routing fail-closed:** review events always review; `issue_comment`/`issues` review iff the code-block-stripped body contains "review".
- **Comment patching** shared in `.github/scripts/patch_claude_comment.sh`+`compose_claude_comment.py` (don't duplicate inline); fetches **all** pages (`--paginate --slurp`).
- **Concurrency:** `group: claude-<issue/PR number>`, `cancel-in-progress: false`.
- **`claude_args` allowlist is defense-in-depth, not a hard boundary** — the bound token above is the real boundary. **No-execution ban:** the workflow agent never runs the project's code in any mode (no test suites, backtests, builds, simulations, or scripts — CI covers those); no interpreters/`go`/`uv run` on the allowlist, no Python env installed (setup-go stays only for `gofmt`), and every prompt file restates the ban. Commit/push rules implement-mode-only, **exact-match** (never trailing-wildcard); `gh api` dropped entirely.
- **Prompt-file injection guard:** `pr-review-format.md` must not contain `"`, `` ` ``, or `$`.
- **CLAUDE.md revision step** must explicitly instruct `git commit`+`git push origin HEAD` (uncommitted edits discarded at runner end). Has stray-reverted working-tree `CLAUDE.md` to `origin/main` before, silently discarding prior branch commits — right after invoking it, diff `HEAD -- CLAUDE.md` and `origin/main -- CLAUDE.md`; matching diffs mean it reverted, not edited — restore pre-existing content first.
- `timeout-minutes: 90`. `issues: types: [opened]` only.

### Addressing review findings
Before implementing `@claude review` findings: restate each item as an invariant, enumerate states that would break the proposed fix (especially the inverse of the reported scenario and compound/concurrent cases), and add tests for that class — not only the reported example — before pushing.

## GitHub Issues
- Create with `gh issue create`; prefix the title with a complexity score: `[C<0-100>] <title>` (e.g. `[C70] Fix order-fill race`).
- **Complexity score (0–100):** an approximation, **not** a time/effort estimate. Weigh **scope** (files/layers/surfaces touched), **risk** (blast radius — money/data-integrity/security/auto-protective paths weigh heaviest), and **uncertainty** (unknowns/research before the work is well-defined).
- First body line is a one-line rationale naming the drivers, number matching the title: `**Complexity: 70/100** — scope: medium; risk: high (order-fill path); uncertainty: exchange API behavior unverified`.
- End the body with the same footer as commits/PRs — `LLM: <model> | <effort> | Harness: <action>` — no `Co-authored-by` trailer.

## Build & Deploy
- **Update:** `bash scripts/update.sh --restart` — atomic: preflight → `pull --ff-only` (or `--rsync-from <src>`, won't clobber `.git/`/config/state DB/venv/binary — DBs guarded by `*.db`/`*.db-wal`/`*.db-shm`/`*.db.lock` extension globs via `update_db_rsync_excludes`, not just named files, #1013) → `uv sync` → build → probe → swap (`.prev`) → restart+verify → rollback on timeout. Env `STATUS_PORT`/`ACTIVE_TIMEOUT=30s`/`HEALTH_TIMEOUT=60s`. **Never rebuild Go alone — argv contract requires both sides (Go+Python) at same SHA.** Restart `--restart-mode systemd` (default; falls back to signal) / `signal` (`GO_TRADER_PIDFILE`/`GO_TRADER_RUN_SH`). Batch `--all --restart` (**#1055** auto-discovers deployments from systemd `WorkingDirectory` of loaded `go-trader`/`go-trader-*`/`go-trader@*` units via `discover_deployment_dirs_from_systemd` in `update_helpers.sh` — layout-independent; `--update-all-root`/`GO_TRADER_UPDATE_ALL_ROOT` pins the legacy `go-trader-*/` glob, also the no-systemd fallback; zero-update batch hard-fails).
- **Graceful shutdown:** drains side-effecting subprocesses ≤ `shutdownDrainCap=15s` then SIGKILLs; state-save/notifier-flush/DB-close after via deferred LIFO. Unit `TimeoutStopSec=20`; service-file changes require `daemon-reload`.
- Build (one-off): `go build -ldflags "-X main.Version=$(git describe --tags --always --dirty=-mod)" -o go-trader .` (rebuild before smoke-testing). Restart `systemctl restart go-trader`; config-only `kill -HUP $(pgrep go-trader)`; Python picks up next cycle.
- **Startup probe:** binary invokes each unique check script with `--probe-only`; non-zero → log + owner DM + `os.Exit(ExitProbeFailure)` (exit 78, `EX_CONFIG`). `probeFailureScriptMissing` detects `"can't open file"` to distinguish missing-script deploy gaps from argparse mismatches. Both service files set `RestartPreventExitStatus=78` so systemd stays down instead of restart-looping. Failures after update → ensure `shared_scripts/` updated + rebuild.
- **Post-update:** follow `SKILL.md` "Post-Update Agent Protocol" (diff `<running>..HEAD`, classify via Reference table, prompt per item). **Post-deploy smoke:** after a Python-launcher change, `./go-trader --config scheduler/config.json --once`.

## Backtest
**Mechanics, harness flags, and #951/#983/#989/#1005 detail → `docs/ARCHITECTURE.md` § Backtest harnesses. Guardrails + run commands below.**
- **Harness registry SSoT → `docs/backtesting-registry.md`** — the map of every `backtest/` tool (core simulators, M1–M6 validators, regime-promotion pipeline, research one-shots). **Any PR that adds/deprecates/repurposes a harness updates its row there in the same PR** (a new row is part of the change, not a follow-up).
- Run: `uv run --no-sync python backtest/run_backtest.py --strategy <n> --symbol BTC/USDT --timeframe 1h --mode single`; `backtest_options.py`/`backtest_theta.py --underlying BTC --since YYYY-MM-DD --capital 10000`; `backtest_pairs.py` (beta-hedged z-score, no live path).
- `--config <path> --strategy <id>` reads the single `close_strategy` and, by default, applies `user_defaults` so effective close params match live; pass `--defaults system` only to preserve an old built-in-default baseline. **#951: `--config` gates on `config_version>=15`** (pre-v15 legacy close keys silently no-op vs live → reject with migrate text). Entry ATR guard 50%-of-AvgCost. **#1271** SL-vs-TP races resolve intra-bar by default (`ohlc_walk`: a range touch on the armed stop exits same-bar at the trigger price, at the open on a gap-through, adverse-move-first over a same-bar TP); `--intrabar-resolution bar_close` restores the legacy bar-close/next-open semantics for pre-#1271 baselines.
- Regime: entries blocked when `bar_regime ∉ allowed_regimes`; closes always execute; options unsupported. **#1025** `--config` threads `allowed_regimes` (CLI flag rejected); named `regime_gate_window` rejected at load when gate active. **#1058** composite 9-state via `regime.windows` (#1124 `ranging_directional_up`/`_down`, bare-covers-subs in the bar-regime gate); **#1067** open name falls back to `args[0]`. **Regime parity detail → ARCHITECTURE.md.**
- **Backtester rejects (HL-live-only, no parity):** `regime_window_divergence` (#951 loud reject at `--config`), `tiered_tp_atr_live_regime_dynamic`. `regime_directional_policy` is backtestable via the #1025 per-cycle resolver, behind the **#1085 evidence gate** (Backtester takes `regime_directional_certified`, nulls the policy unless certified, mirroring live default-off). Scalar `sl_after.trail_from_here.tp_atr_fraction` + `*_atr_regime` dicts backtestable. **Detail → ARCHITECTURE.md.**
- **Look-ahead invariants:** signal at bar N fills at N+1 open; regime gate reads N-1 regime; closes use closed-bar ATR. Regression `backtest/tests/test_backtester_lookahead.py`. **Any HTF/coarser-timeframe series in `run_backtest.py` indexed by candle OPEN time (`_htf_trend_series`, `_aligned_regime_columns`, `_profile_label_series`) MUST `.shift(1)` BEFORE `reindex(ltf_index, method="ffill")`** — else an LTF bar inside a still-forming HTF candle leaks that candle's final close (up to one full HTF period; #1153 regime/profile, #1154 HTF trend). Regressions in `test_backtest_reporting.py`; corrected baselines in `docs/research/1181-htf-filter-baseline-revalidation.md`. M1/M2 never wire `htf_filter`; strategies with their own HTF gating (`mtf_confluence`, `regime_adaptive_htf`, `chart_pattern`) bypass `_htf_trend_series` — unaffected.
- **#1005 liquidation floor:** `_calculate_metrics` sticky-floors equity at 0 from the first bust bar (negative-base `pct_change` inverts Sharpe/Sortino sign); blown legs floored to `±LIQUIDATED_METRIC_FLOOR`, propagated by `eval_windows`/`fee_audit`.
- **M1–M6 research harnesses** (audit slices via `eval_windows.py`; **detail → ARCHITECTURE.md**): M1 incumbent-relative validator; M1 step-2 (#1054) `gross_edge_noise.py` — noise gate for `graduate_m1` verdicts, run BEFORE any selectivity work; M2 joint open×exit sweep; M3 exit diagnostics; M5 fee audit; M6 (#1066) regime exit A/B. **#983/#984** close-sweep negative results; **#1054** regime_adaptive_htf gross edge = noise → deprecate. Row-level registry → `docs/backtesting-registry.md`.
- **#1210 auto-suggester `backtest/auto_suggest.py`** — reusable cross-harness driver: declarative `suggest.json` spec (extends `--candidate-json` + `backtest/candidates/<study>/`) → parallel M-harness shelling → one Benjamini–Hochberg pass over the whole candidate family → ranked survivor artifact. **SUGGEST-ONLY — never writes a live default/config/PR**; a human makes every promotion call. Run: `uv run --no-sync python backtest/auto_suggest.py --spec backtest/candidates/<study>/suggest.json [--dry-run]`. **Mechanics (M6 `baseline_config`/`incumbent_close` exactly-one rule, BH correction scope, noise-family dedup) → ARCHITECTURE.md.**
- **Regime promotion (#1082/#1074, offline pipeline #1065–#1097):** bounded-window ADX re-validation + vol-model/economic/multi-asset gates — **mechanics → ARCHITECTURE.md**.

## Testing
- **New functionality must include tests.** Go `_test.go`; Python `test_*.py`; regression-test bug fixes. Extract pure helpers from subprocess wrappers — Go CI must not depend on spawning Python (`perpsLiveOrderSize`,`*OrderSkipReason`,`parseXxxCloseOutput`, Sharpe testable without subprocesses).
- `uv run --no-sync python -m py_compile <file>` from repo root. `go build -ldflags "…" .` / `go test ./...` from repo root (not `cd scheduler`); `gofmt -w <file>.go` post-edit. Multi-line Go edits with tabs may fail Edit tool; use Python heredoc with `read()`+`replace(old,new,1)`+`write()`.
- Strategy listing `…/open/{spot,futures}/strategies.py --list-json`; smoke `./go-trader --once`; JSON init `./go-trader init --json '{...}' --output /tmp/test.json`.
- Pytest `uv run --no-sync python -m pytest shared_strategies/ shared_tools/ platforms/ backtest/`; `shared_scripts/test_*.py` NOT in `testpaths` (invoke explicitly). **Registry/sys.path tests → run the FULL suite** (isolated runs mask open-vs-close `registry.py` import-order conflicts); test imports use `importlib.util.spec_from_file_location`. **#1304 CI runs pytest with `-n auto` (pytest-xdist)** — per-worker order is nondeterministic: never bare-`import` an ambiguous module name in a test (load by path via `spec_from_file_location`); treat an intermittent CI pytest failure as a test-isolation bug to fix, never a flake to re-run. Go tests check `json.Unmarshal` errors.
- `stampEntryATRIfOpened` rejects ATR > 50% of AvgCost; Go tests using `atr` must use values < 50% of entry price. Strategy tests must assert actual signal values; smoke tests iterating registered strategies need `DatetimeIndex` (`amd_ifvg` reads `index.hour`, `vwap_reversion` buckets by `index.date`).
- **ATR for close evaluators:** `tiered_tp_atr`/`trailing_stop_atr_mult` need `Position.EntryATR` (`ensure_atr_indicator` before loop); `tiered_tp_atr_live` recomputes from `market_ctx["atr"]` (`atr_source` `live`|`entry`). `atr_stop` (#997) and `avwap_stop` (#1196) follow the same `atr_source` rule. **Tiered-TP dust:** sole-peer final-tier `market_close(sz=None)`; `compute_tp_tier_sizes` pre-floors non-final. `avwap_stop` reads a **live re-anchored** `market["avwap"]` (no `Position`-carried anchor) — **virtual exit only**, not in `isTieredTPATRCloseName`/`closeStrategiesSuppressedByOnChainProtection`; `evaluate_open_close`/`Backtester` warn once when configured but no usable `avwap` column is ever injected.
